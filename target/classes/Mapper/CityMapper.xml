<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qf.dao.CityMapper">

<select id="findById" resultType="com.qf.pojo.Scenic">
    SELECT city.`city_name`,scenic.`scenic_name` FROM city,scenic,city_scenic WHERE city.`id`=city_scenic.`city_id` AND scenic.`id`=city_scenic.`scenic_id` AND city.`id`=#{id}
</select>

</mapper>